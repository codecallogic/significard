const initialState = {
  recipient: ''
}

export const recipientReducer = (state = initialState, action) => {
  switch (action.type) {
    case "UPDATE_STATE":
      
      break;
  
    default:
      return state
  }
}